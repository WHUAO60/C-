<Window x:Class="NewsAggregator.RssSourceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="📡 订阅源管理" Height="450" Width="650"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}"
        FontFamily="Microsoft YaHei UI" FontSize="13">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📡 管理你的订阅源" FontSize="18" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Margin="0,0,0,15" Padding="16"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8">
            <StackPanel>
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="网站名称" VerticalAlignment="Center"
                               Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBox x:Name="TxtSourceName" Grid.Column="1"
                             Style="{StaticResource ModernTextBoxStyle}"/>
                </Grid>
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="RSS 地址" VerticalAlignment="Center"
                               Foreground="{StaticResource TextSecondaryBrush}"/>
                    <TextBox x:Name="TxtSourceUrl" Grid.Column="1"
                             Style="{StaticResource ModernTextBoxStyle}"/>
                </Grid>
                <Button Content="➕ 添加/更新" Style="{StaticResource PrimaryButtonStyle}"
                        Width="120" HorizontalAlignment="Right" Click="BtnAddOrUpdate_Click"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="2" Text="已添加的订阅源" FontWeight="SemiBold" Margin="0,0,0,8"
                   Foreground="{StaticResource TextPrimaryBrush}"/>

        <ListView Grid.Row="3" x:Name="LvSources" Margin="0,0,0,15"
                  Style="{StaticResource ModernListViewStyle}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="启用" Width="60">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsEnabled}" HorizontalAlignment="Center"
                                          Checked="ChkEnabled_Changed" Unchecked="ChkEnabled_Changed"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="网站名称" DisplayMemberBinding="{Binding Name}" Width="150"/>
                    <GridViewColumn Header="RSS 地址" DisplayMemberBinding="{Binding Url}" Width="350"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnDelete" Content="🗑️ 删除选中" Width="100"
                    Style="{StaticResource DangerButtonStyle}" Click="BtnDelete_Click" Margin="0,0,10,0"/>
            <Button x:Name="BtnClose" Content="关闭" Width="70"
                    Style="{StaticResource SecondaryButtonStyle}" Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>